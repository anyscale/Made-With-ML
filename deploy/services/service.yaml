name: service-mlops
project_id: prj_8tqvub3w9wherks256xlra9ch4
compute_config: madewithml-compute-config
cluster_env: madewithml-cluster-env:4
ray_serve_config:
  import_path: deploy.services.service:entrypoint
  runtime_env:
    working_dir: .
    upload_path: s3://goku-mlops/workingdir/service
    env_vars:
      EXPERIMENT_NAME: llm
